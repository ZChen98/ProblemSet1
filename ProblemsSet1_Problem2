"""
Justin Chen
Problem 2: Basic Operations
"""

# 1. Multiply two vairables and format the answer to two decimals
deposite_amounts = 1000
interest_rate = 0.05
total_wealth = deposite_amounts * (1 + interest_rate * 10)
print("Bill's total wealth is ${:.2f}".format(total_wealth))

# 2. Double the deposite amount, move variables in the simple interest formula to get the time needed
total_wealth = deposite_amounts * 2
time_needed = (total_wealth / deposite_amounts - 1) / interest_rate
print("It takes {:.2f} years to double Bill's money".format(time_needed))

# 3. Modify the values of the variables, compare the total wealth and the desired amount
deposite_amounts = 5000
interest_rate = 0.2
total_wealth = deposite_amounts * (1 + interest_rate * 6)
print(total_wealth == (deposite_amounts * 2))

# 4. Created a list with names to be string value and money to be integer value
client_accounts = ["Bill", 1000, "Jack", 5000, "Amy", 6700, "Cindy", 5699, "Harry", 6700]
print(client_accounts)

# 5. Loop through the list and use every other value to be keys and values 
Accounts = {client_accounts[i]: client_accounts[i + 1] for i in range(0, len(client_accounts), 2)}
print(Accounts)

# 6. Loop through the dictionary using the key as the first value of the tuple and value as the second value of the tuple
account_tuples = [(client, money) for client, money in Accounts.items()]
print(account_tuples)

# 7. 
# List is a collection of ordered data
# Dictionary is a collection of unordered data that is represented in key-value pairs
# Sets are unordered collections of data 
